---
name: Bug report
about: Something behaves differently than the docs/tests say it should
title: ''
labels: bug
assignees: ''
---

**roleplay-slim version** (`pip show roleplay-slim`):

**What happened:**

**What you expected:**

**Your `CompressorConfig` (or the relevant `[compressor]` TOML section)**
— synthetic values are fine, we don't need your actual persona text:

```toml

```

**A minimal message array that reproduces it**, if you can construct one.
If the bug depends on your specific content shape and you can't share it,
describe the shape instead (roles, which messages repeat across turns,
multimodal content, tool calls):

```json

```
