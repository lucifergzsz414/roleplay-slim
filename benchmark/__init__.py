"""Synthetic benchmark tooling for roleplay-slim.

Nothing here runs in the library or the proxy — it is dev/benchmark-only
tooling, kept in its own package so tests can import it
(``from benchmark.corpus_gen import generate``).
"""
